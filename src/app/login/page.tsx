import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Login",
    description: "abc",
};

export default function Login() {
    return (
        <>
            <div>Login !!!!!!</div>
        </>
    );
}
